# Architecture Decision Records

ADR naming convention:

- `ADR-0001-*` for initial program decisions
- one file per decision
- immutable decision statement with status updates only

Initial ADR backlog is defined in `../roadmap/adr-backlog.md`.

Current ADRs:

- `ADR-0001` - Program foundation
- `ADR-0002` - GitHub operating system bootstrap
